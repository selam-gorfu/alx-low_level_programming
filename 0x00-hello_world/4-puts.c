#include <stdio.h>

/**
 * main - main entry
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char statement[] = "\"Programming is like building a multilingual puzzle";

	puts(statement);

	return (0);
}
